# =============================================================================
# HB1603 Acoustic Survey — Simplified End-to-End Pipeline Recipe
# =============================================================================
# Simplified variant of hb1603_survey_pipeline.yaml that drops the parallel
# uncalibrated baseline Sv branch. A single calibrated Sv is used for both
# mask construction (seafloor/surface/frequency) and downstream science
# (noise removal, MVBS, ML). Use the full recipe if you need a side-by-side
# calibrated vs. uncalibrated comparison.
#
# Usage: aa-recipe generate simplified_hb1603_survey_pipeline.yaml

recipe:
  name: "hb1603_survey_pipeline_simplified"
  description: "Simplified EK60 acoustic survey pipeline for cruise HB1603 (single calibrated Sv)."
  version: "1.0"
  author: "<Your Name>"
  schema_version: "1"

# ---------------------------------------------------------------------------
# Pipeline-level inputs
# ---------------------------------------------------------------------------
inputs:
  raw_input_folder:
    type: path
    default: "./raw_file_inputs"
  cal_input_folder:
    type: path
    default: "./calibration_files/HB201607_cal"
  line_file_path:
    type: path
    default: "./line_files/SpermWhaleClicks_click_data_HB1603_SpermWhaleDive_Span0.2_07252016_2120_UTC.csv"

# ---------------------------------------------------------------------------
# Steps
# ---------------------------------------------------------------------------
steps:

  # --------------------------------------------------------------------------
  # Data retrieval
  # --------------------------------------------------------------------------
  - id: query_ncei
    op: query_ncei_data
    params:
      file_time_start: "2016-07-25T20:58"
      file_time_end: "2016-07-25T21:45"
      filters:
        CRUISE_NAME: "HB1603"

  - id: download_raw
    op: download_ncei_data
    inputs:
      results: ${query_ncei.ncei_results}
      query_label: ${query_ncei.query_label}
    params:
      output_dir: ${inputs.raw_input_folder}
      companion_extensions:
        - ".bot"
        - ".idx"

  # --------------------------------------------------------------------------
  # File setup and calibration mapping
  # --------------------------------------------------------------------------
  # Both steps point at the per-query subfolder created by download_raw
  # (${download_raw.download_dir}) so re-running with a different query
  # writes into a sibling folder instead of mixing files.
  - id: initial_setup
    op: initial_setup
    params:
      raw_input_folder: ${download_raw.download_dir}
      raw_file_names: []
      clear_previous_json_logs: true

  - id: gen_cal_mapping
    op: generate_standardized_cal_mapping
    params:
      raw_input_folder: ${download_raw.download_dir}
      cal_input_folder: ${inputs.cal_input_folder}
      output_base: ${initial_setup.calibration_output_dir}
      cruise_id: "HB1603"
      record_author: "<Your Name>"
      short_filenames: true
      keep_unused: true
      conflict_resolution: "error"

  # --------------------------------------------------------------------------
  # Load raw data and extract calibration parameters
  # --------------------------------------------------------------------------
  - id: read_raw
    op: read_raw_files
    inputs:
      raw_file_paths: ${initial_setup.raw_file_paths}
    params:
      sonar_model: "EK60"
      include_bot: true

  - id: combine_raw
    op: combine_raw_files
    checkpoint: always
    inputs:
      raw_stores: ${read_raw.raw_stores}

  - id: extract_cal_params
    op: extract_standardized_cal_params
    inputs:
      calibration_dict: ${gen_cal_mapping.calibration_dict}
      mapping_dict: ${gen_cal_mapping.mapping_dict}
      echodata: ${combine_raw.echodata}

  # --------------------------------------------------------------------------
  # Compute Sv (single calibrated Sv used everywhere downstream)
  # --------------------------------------------------------------------------
  - id: compute_sv
    op: compute_sv
    inputs:
      echodata: ${combine_raw.echodata}
      cal_params: ${extract_cal_params.cal_params}
      env_params: ${extract_cal_params.env_params}

  # --------------------------------------------------------------------------
  # Seafloor detection (stats first, then detection)
  # --------------------------------------------------------------------------
  - id: log_seafloor_stats
    op: log_seafloor_detection_stats
    inputs:
      echodata: ${combine_raw.echodata}
      ds_Sv: ${compute_sv.ds_Sv}
    params:
      min_valid_depth_m: 10.0

  - id: detect_seafloor
    op: detect_seafloor
    depends_on: [log_seafloor_stats]
    inputs:
      ds_Sv: ${compute_sv.ds_Sv}
      echodata: ${combine_raw.echodata}
    params:
      min_valid_depth_m: 10.0

  # --------------------------------------------------------------------------
  # Mask construction
  # --------------------------------------------------------------------------
  - id: create_seafloor_mask
    op: create_seafloor_mask
    inputs:
      ds_Sv: ${compute_sv.ds_Sv}
      seafloor_depth: ${detect_seafloor.seafloor_depth}
    params:
      seafloor_buffer_m: 100

  - id: create_surface_mask
    op: create_surface_mask
    inputs:
      ds_Sv: ${compute_sv.ds_Sv}
    params:
      surface_depth_m: 10.0

  - id: create_frequency_mask
    op: create_frequency_mask
    inputs:
      ds_Sv: ${compute_sv.ds_Sv}
    params:
      frequencies_to_mask: [70, 120, 200]

  - id: combine_masks
    op: combine_masks
    inputs:
      masks:
        - ${create_seafloor_mask.mask}
        - ${create_surface_mask.mask}
        - ${create_frequency_mask.mask}
    params:
      mode: "and"

  # --------------------------------------------------------------------------
  # Apply mask
  # --------------------------------------------------------------------------
  - id: apply_mask
    op: apply_sv_mask
    inputs:
      ds_Sv: ${compute_sv.ds_Sv}
      mask: ${combine_masks.mask}

  # --------------------------------------------------------------------------
  # Noise removal, MVBS, and line overlay
  # --------------------------------------------------------------------------
  - id: remove_noise
    op: remove_background_noise
    inputs:
      ds_Sv: ${apply_mask.ds_Sv}
    params:
      noise_range_sample_num: 10
      noise_ping_num: 5

  - id: compute_cell_stats
    op: compute_per_cell_statistics
    inputs:
      ds_Sv: ${remove_noise.ds_Sv}
    params:
      range_bin: &bin_range "2m"
      ping_time_bin: &bin_time "10s"
      statistics:
        - cv

  - id: mask_sparse
    op: mask_sparse_bins
    inputs:
      ds_Sv: ${remove_noise.ds_Sv}
    params:
      range_bin: *bin_range
      ping_time_bin: *bin_time
      nan_threshold: 0.6

  - id: compute_mvbs
    op: compute_mvbs
    inputs:
      ds_Sv: ${mask_sparse.ds_Sv}
    params:
      range_bin: *bin_range
      ping_time_bin: *bin_time

  - id: add_line_overlay
    op: add_line_overlay
    inputs:
      ds: ${compute_mvbs.ds_MVBS}
    params:
      line_file_path: ${inputs.line_file_path}
      line_name: "sw_dive_profile"

  # --------------------------------------------------------------------------
  # Echogram visualization
  # --------------------------------------------------------------------------
  - id: plot_sv_clean
    op: plot_sv_echogram
    inputs:
      ds_Sv: ${mask_sparse.ds_Sv}
    params:
      min_depth: 0
      max_depth: 1800
      ping_min: 0
      ping_max: 515
      x_axis_units: "seconds"
      y_axis_units: "meters"

  - id: plot_mvbs
    op: plot_sv_echogram
    inputs:
      ds_Sv: ${add_line_overlay.ds}
      ds_Sv_source: ${mask_sparse.ds_Sv}
    params:
      min_depth: 0
      max_depth: 1800
      ping_min: 0
      ping_max: 515
      x_axis_units: "seconds"
      y_axis_units: "meters"
      overlay_lines:
        - var: "sw_dive_profile_fit"
          style:
            color: "red"
            linewidth: 3.5

  # --------------------------------------------------------------------------
  # Machine learning: reshape → auxiliary features → normalize → plot → cluster
  # --------------------------------------------------------------------------
  - id: reshape_ml
    op: reshape_for_ml
    inputs:
      ds_MVBS: ${add_line_overlay.ds}
    params:
      data_var: "Sv"
      dataset_name: "ml_dataset"
      feature_strategy: "mean_centered"
      baseline_channel: 0


  - id: normalize_ml
    op: normalize_ml_data
    inputs:
      ds_ml: ${reshape_ml.ds_ml_ready}
    params:
      method: "flatten"
      dataset_name: "ml_dataset"
      normalization_name: "normalized_flatten_mean_centered"
      shift_positive: false
      per_group_methods:
        depth: "minmax"

  - id: plot_normalized_ml
    op: plot_ml_echogram
    inputs:
      ds_normalized: ${normalize_ml.ds_normalized}
      ds_Sv: ${mask_sparse.ds_Sv}
    params:
      dataset_name: "ml_dataset"
      normalization_name: "normalized_flatten_mean_centered"
      min_depth: 0
      max_depth: 1800
      ping_min: 0
      ping_max: 515
      x_axis_units: "seconds"
      y_axis_units: "meters"

  - id: run_hdbscan
    op: run_hdbscan
    inputs:
      ds_normalized: ${normalize_ml.ds_normalized}
    params:
      dataset_name: "ml_dataset"
      normalization_name: "normalized_flatten_mean_centered"
      ml_result_name: "hdbscan_results"
      min_cluster_size: 1400
      min_samples: 4
      sample_size: 1000000
      cluster_selection_method: "eom"
      use_hdbscan: true

  - id: embed_results
    op: embed_clustering_results
    inputs:
      ds_normalized: ${normalize_ml.ds_normalized}
      clustering_results: ${run_hdbscan.clustering_results}
    params:
      dataset_name: "ml_dataset"
      ml_result_name: "hdbscan_results"

  - id: plot_clustering_report
    op: plot_clustering_report
    inputs:
      ds_normalized: ${embed_results.ds_normalized}
      clustering_results: ${run_hdbscan.clustering_results}
      ds_Sv: ${mask_sparse.ds_Sv}
    params:
      dataset_name: "ml_dataset"
      ml_result_name: "hdbscan_results"
      plot_window: [0, 1800, 0, 515]
      overlay_line_var: "sw_dive_profile"
      cluster_colors:
        - "#F700FF"
        - "#35E200"
        - "#5EFC9B"
        - "#2F00FF"
        - "#E60000"
        - "#FFFB1C"
        - "#000F92"
        - "#E70000"
        - "#00948D"
        - "#5BECFFFF"
        - "#4100B9FF"
